# Database Configuration
DB_USER=taskmanagement_user
DB_PASSWORD=taskmanagement_password
DB_NAME=taskmanagement_db
DB_PORT=5432

# API Configuration
API_PORT=5000
ASPNETCORE_ENV=Production

# Client Configuration
CLIENT_PORT=80

# CORS Configuration (added to API env vars)
CORS_ALLOWED_ORIGINS=http://localhost:80,http://localhost:8080,http://client:80
